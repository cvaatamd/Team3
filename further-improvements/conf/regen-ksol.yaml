# Regenerate KSOL runs WITH persisted predictions (Patch 1). Mirrors conf/fullexp-ksol.yaml.
target_endpoints:
  - "KSOL"
arms: ["baseline", "intra_task", "external", "both"]
n_grid: [50, null]
seeds: [0, 1, 2, 3, 4]
mechanism_by_arm:
  baseline: "gbm_baseline"
  intra_task: "mt_cotrain"
  external: "mt_cotrain"
  both: "mt_cotrain"
exclude_flagged_slices: true
